dependencies:
  flutter:
    sdk: flutter
  camera: ^0.10.5
  hand_detection: ^0.1.0        # MediaPipe untuk Flutter
  google_ml_kit: ^0.15.0        # Alternatif ML Kit
  webview_flutter: ^4.4.0       # Untuk integrasi (jika perlu)
